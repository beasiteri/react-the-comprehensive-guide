import './App.css';

function App() {
  return (
    <main className="flex min-h-screen items-center justify-center px-6">
      <div className="max-w-2xl text-center">
        <h1 className="text-4xl font-bold"> React + Vite + MSW Boilerplate </h1>
        <p className="mt-4 text-gray-600">
          A reusable React starter with TypeScript, Tailwind CSS, Material UI, Axios, and Mock
          Service Worker.
        </p>
        <p className="mt-2 text-sm text-gray-500">
          Browser-based API mocking for local development and static hosting.
        </p>
      </div>
    </main>
  );
}

export default App;
